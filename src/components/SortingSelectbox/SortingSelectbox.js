import React from 'react';

function SortingSelectbox() {
  return (
    <select className="sorting-selectbox">
      <option value="sort-by-age" className="sorting-selectbox__option"></option>
    </select>
  );
}

export default SortingSelectbox;
